server:
  port: 8761

eureka:
  instance:
    hostname: localhost
    datacenter: upc
    environment: dev
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/